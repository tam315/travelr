const INITIAL_STATE = {
  userId: null,
  token: null,
  displayName: null,
  isAdmin: null,
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
